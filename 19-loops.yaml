- name: Install and remove packages in loops 
  hosts: web
  become: yes
  tasks:
  - name: Install Packages 
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: 'git', state: 'present'}
    - { name: 'mysql', state: 'absent'}
    - { name: 'gcc', state: 'present'}
    - { name: 'zip', state: 'absent'}