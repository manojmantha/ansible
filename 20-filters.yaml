- name: filters
  hosts: local
  connection: local
  tasks:
  - name: Handle the undefined variable
    ansible.builtin.debug:
      msg : "Hello {{ person | default('Manoj')}}"
  - name: Convert String to list 
    vars:
      fruits: "apple, banana, orange" # String
    ansible.builtin.debug:
      msg: "{{ fruits | split(',')}}"
    