- name: shell vs Command
  hosts: local
  connection: local
  tasks:
  - name: execute ls Command
    ansible.builtin.command: ls 
    register: ans 
  - name: print
    ansible.builtin.debug:
      msg: "{{ ans }}"