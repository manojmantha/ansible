- name: start and run nginx
  hosts: web
  become: yes # -b 
  tasks:
  - name: installing nginx 
    ansible.builtin.package:
      name: nginx
      state: present
  - name: running nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes